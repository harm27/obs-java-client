name: Auto-Create branch

on: 
  issues:
    types: [labeled]

jobs:
  branch:
    runs-on: ubuntu-latest
    if: github.event.label.name == 'in-development'

    steps:
    - uses: actions/checkout@v2

    - name: Debugging
      run: 
        echo Debug ${{github.event.label.name }}
